export enum AigInputKind {
    /**
     * I'm not sure if infer is a kind
     */
    InferType = 'inferType',
    String = 'string',
    Number = 'number',
    Boolean = 'boolean',
    Object = 'object',
    /**
     * @deprecated
     */
    Group = 'group',
}
