export enum AigType {
    /**
     * This is not a primitive type, but a type that is used to represent a user type.
     */
    UserType = 'userType',
    String = 'string',
    Number = 'number',
    Boolean = 'boolean',
    Object = 'object',
    /**
     * @deprecated
     */
    Group = 'group'
}
