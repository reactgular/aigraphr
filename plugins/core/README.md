# nodes

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nodes` to build the library.

## Running unit tests

Run `nx test nodes` to execute the unit tests via [Jest](https://jestjs.io).
